require "bank"

describe Bank do

  # let(:account) { mock('account') }
  # # it { expect(subject).to respond_to(statement) }
  # #
  # # it "should be able to check statement" do
  # #   expect(subject.statement).to eq "date || credit || debit || balance"
  # # end
  # it {expect(subject).to respond_to(:deposit).with(1).argument}
  #
  # it "should be able to deposit money into bank" do
  #   subject.deposit(10)
  #   expect(subject.balance).to eq(10)
  # end
end
